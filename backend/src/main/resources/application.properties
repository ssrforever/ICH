# 1. 数据库连接配置 (确保 feiyi_db 数据库已经在 MySQL 中创建了)
spring.datasource.url=jdbc:mysql://localhost:3306/feiyi_db?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=Ljx083495#

# 2. 脚本初始化配置
# 总是执行初始化脚本
spring.sql.init.mode=always
# 告诉 JPA 不要自动建表（因为我们用了 schema.sql）
spring.jpa.hibernate.ddl-auto=none